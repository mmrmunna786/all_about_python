CREATE TABLE dogs ( -- creating table called "dogs" with its data members.
    name TEXT,
    breed TEXT,
    age INTEGER
); -- note the semicolon at the end

CREATE TABLE cats ( -- creating table called "cats" with its data members.
    name TEXT,
    breed TEXT,
    age INTEGER
); -- note the semicolon at the end

INSERT INTO cats ( -- inserting into "cats" table.
    name,
    breed,
    age
 ) VALUES ( 
    "Blue",
    "Scottish Fold",
    3
); -- note the semicolon at the end

